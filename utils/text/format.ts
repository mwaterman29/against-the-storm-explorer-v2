import { interpolateSprites } from "./interpolateSprites";

const formatText = (text: string) => 
{
    // Interpolate sprites
    let result = interpolateSprites(text);

    // Interpolate meaningful text
    //

    return result;
}

export { formatText };